import { CodeHigh } from './code-high';

describe('CodeHigh', () => {
  it('should create an instance', () => {
    const directive = new CodeHigh();
    expect(directive).toBeTruthy();
  });
});
